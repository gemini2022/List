import { ColumnResizerDirective } from './column-resizer.directive';

describe('ColumnResizerDirective', () => {
  it('should create an instance', () => {
    const directive = new ColumnResizerDirective();
    expect(directive).toBeTruthy();
  });
});
